<p id="notice"><%= notice %></p>


<h1>preguntas</h1>

<table>
  <thead>
    <tr>
      <th>Texto</th>
        <th>Respuesta 1</th>
          <th>Respuesta 2 </th>
            <th>Respuesta 3 </th>
              <th>Respuesta 4 </th>

      <th>Tiempo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @preguntas.each do |pregunta| %>
      <tr>
        <td><%= pregunta.texto %></td>
          <td><%= pregunta.respuesta1 %></td>
            <td><%= pregunta.respuesta2 %></td>
              <td><%= pregunta.respuesta3 %></td>
                <td><%= pregunta.respuesta4 %></td>

        <td><%= pregunta.tiempo %></td>
        <td><%= link_to 'Show', admin_pregunta_path(pregunta) %></td>
        <td><%= link_to 'Edit', edit_admin_pregunta_path(pregunta) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @preguntas,  :previous_label => 'Atrás', :next_label => 'Adelante'%>
<br>

<%= link_to 'Nueva pregunta', new_admin_pregunta_path %>
